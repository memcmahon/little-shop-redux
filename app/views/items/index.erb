<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8" />
    <title>Shop With No Name</title>
    <link href="https://fonts.googleapis.com/css?family=PT+Sans+Narrow" rel="stylesheet">
    <link rel="stylesheet" href="stylesheets/main.css" title="text/css" />
  </head>

  <body>
    <div class='banner'>
      <header>
        <h1>Items</h1>
      </header>
      <nav>
          <a class='nav-links' href="/">Home</a>
          <a class='nav-links' href="/merchants">Merchant Index</a>
          <a class='nav-links' href="/categories">Category Index</a>
      </nav>
    </div>

    <section class='create-new'>
      <a class='body-link' href='/items/new'>Create New Item</a>
    </section>

    <section class="display-grid">
      <% @items.each do |item| %>
        <div>
        <div class='cell-title'><h3><a class='body-link' href="/items/<%= item.id %>"><%= item.title %></a></h3></div>
          <img class='item-image' src="<%= item.image %>"><br>
          <a href="/items/<%= item.id  %>/edit">Edit</a>
          <form id='delete' action="/items/<%= item.id %>" method="POST">
            <input type="hidden" name="_method" value="DELETE">
            <input type="submit" value="delete"/>
          </form>
        </div>
      <% end %>
    </section>

    <footer>
      <p>Email Us: <a class='nav-links' href="mailto:fakeaddress@gmail.com">HR@shopwithnoname.com<a></p>
    </footer>

    <div>
    </div>
  </body>
</html>
